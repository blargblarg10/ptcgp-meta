/**
 * Default values for optional fields
 */

// Default values for optional fields
export const DEFAULT_VALUES = {
  id: null, // Will be generated
  isLocked: true,
  notes: "",
  points: 0,
  auto: true
};
